/**
 * Maximum file size allowed for uploads in bytes.
 * (5MB = 5 * 1024 * 1024 bytes)
 */
export const MAX_FILE_SIZE = 5 * 1024 * 1024;

/**
 * Supported MIME type for PDF files for validation and processing.
 */
export const SUPPORTED_PDF_MIME_TYPE = 'application/pdf';

/**
 * Supported MIME type for TXT files for validation and processing.
 */
export const SUPPORTED_TXT_MIME_TYPE = 'text/plain';